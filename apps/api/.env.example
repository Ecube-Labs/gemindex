# This file is an example and will NOT be loaded automatically.
# Copy this file to .env and fill in the values.
# All .env and .env.* files (except .env.example) are loaded automatically.
#
# Set DOTENV_PATH to specify a custom directory for .env files.
# Example: DOTENV_PATH=/path/to/env/files

PORT=4000
GEMINI_API_KEY=your_gemini_api_key_here

# Comma-separated list of store IDs that cannot be deleted
# Example: PROTECTED_STORES=abc123,xyz789
PROTECTED_STORES=
